node_modules/
dist/
build/
.next/
coverage/
*.log

# Local tool config (user-specific, not shared)
.claude/settings.local.json

# OS metadata
.DS_Store

# Secrets and credentials
.env*
*.pem
*.key

# Binary files
*.pdf

# Default artifact output (when ARTIFACT_DIR is not set)
artifacts/

# User-specific skill manifests (e.g. private.manifest.json). The shared
# manifest.json stays tracked; only *.manifest.json variants are ignored.
skills-personal/*.manifest.json
