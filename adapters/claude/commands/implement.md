Use `dev-workflow` to run the build pipeline for this task:
`tech-incept` -> `write-plan` -> `execute-plan`.
Prepend `incept` if no requirements exist yet. Use `tech-discovery` if the
technology is unfamiliar. Require plan approval before `execute-plan`.
